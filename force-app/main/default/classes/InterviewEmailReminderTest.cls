@IsTest
public with sharing class InterviewEmailReminderTest {
    
    @IsTest
    static void handleEmailReminder_emailSentAsynchronously_expectSuccess() {
        // Arrange

        // Act

        // Assert
    }
}